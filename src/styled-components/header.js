import styled from 'styled-components';

const Header = styled.div`
  display: block;
  grid-area: header;
  justify-self: center;
  text-align: center;

  img {
    max-height: 90%;
  }
`;

export default Header;

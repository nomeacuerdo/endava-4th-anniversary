import styled from 'styled-components';

const Header = styled.div`
  display: block;
  grid-area: header;
  justify-self: center;
  text-align: center;
  padding: 24px;

  img {
    height: 60px;
  }
`;

export default Header;

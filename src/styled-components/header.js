import styled from 'styled-components';

const Header = styled.div`
  display: flex;
  grid-area: header;
  justify-self: center;
  text-align: center;
  padding: 24px;

  img {
    
  }
  .App-logo {
    height: 60px;
    margin-right: 30px;
  }
  .Anniversary-logo {
    height: 95px;
  }
`;

export default Header;
